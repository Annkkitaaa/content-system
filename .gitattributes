# Normalise line endings. Development happens on Windows, CI runs on Linux,
# and without this the style linter and format check disagree across the two.
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf

*.xlsx binary
*.png binary
*.jpg binary
*.db binary
